
# Memoization in React - Best pratices

**Correct way to use useMemo** 

```js
    const value = useMemo (
() => numbers. reduce((a, v) => a + v, 0),
[numbers]
);
```

**Bad way to use**
```js
const value = useMemo (
'(
=> number1 + number2,
[numberi, number2]
```
**Good**
```js
const value = numberl + number2;
```



